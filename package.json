{
  "name": "zetsu",
  "private": true,
  "scripts": {
    "dev": "sh .bin/run.sh dev",
    "build": "sh .bin/run.sh build",
    "app": "sh .bin/play.sh app"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-check-updates": "^10.2.5"
  }
}
